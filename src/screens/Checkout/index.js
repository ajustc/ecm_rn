import React from 'react';
import {
  StyleSheet,
  Text,
  View,
  Button,
  ScrollView,
  SafeAreaView,
  TouchableOpacity,
} from 'react-native';

const Checkout = ({route}) => {
    console.log({route});
  return (
    <View>
      <Text>Checkout</Text>
    </View>
  );
};

export default Checkout;
